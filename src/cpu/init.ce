bring Template.Intrinsics;

extend jumptarget fun KernelMain() -> void;

entrypoint unmanaged expose noreturn fun _start() -> void {
    DIS();

    WRSTK(0x20000000);
    WRFR(0);
    jump KernelMain();
}